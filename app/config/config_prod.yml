imports:
    - { resource: config.yml }

doctrine:
    orm:
        metadata_cache_driver: apc
        auto_generate_proxy_classes: false
#        result_cache_driver: apc
#        query_cache_driver: apc
## Lignes à ajouter pour optimiser les performances de l'application
monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
        nested:
            type:  stream
            path:  "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
        console:
            type:  console
